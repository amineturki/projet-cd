
- name: "just execute a ls -lrt command"
  shell: "whoami"
  register: "output"
  become: yes
  become_user: cd-amine
- debug: var=output.stdout_lines

- name: "just execute a ls -lrt command"
  shell: "pwd"
  register: "output"
  become: yes
  become_user: cd-amine
- debug: var=output.stdout_lines

- name: deploy app
  shell: kubectl  apply -f deployment-angular.yml
  become: yes
  become_user: cd-amine
# tasks file for kubernetes
